{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "chrome",
            "request": "launch",
            "name": "Launch Chrome against localhost",
            "url": "http://localhost:3000", // Update to match your React app's URL
            "webRoot": "${workspaceFolder}/src" // React apps typically use 'src' as the root
        }
    ]
}
